export type PostLinkType = {
    id: string
    title: string
}

export type User = {
    id: number
    name: string
  }

  export type Project = {
    id: number
    name: string,
    banner: string,
    content: string
  }